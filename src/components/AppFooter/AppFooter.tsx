import styles from './AppFooter.module.scss'

// Экспортируем функциональный компонент AppFooter
export const AppFooter = () => {
    return (
        <footer className={styles['app-footer']} />
    )
}
